package runall;


import java.io.ByteArrayInputStream;
import java.io.ObjectInputStream;
/*
 * import org.hibernate.Session;
import org.hibernate.SessionFactory;
import org.hibernate.cfg.Configuration;

 */
public class ReadClient1 {
	public static void main(String[] args)throws Exception {
/*
 * Configuration cfg=new Configuration().configure("hibernate.anno.cfg.xml");
		
		SessionFactory factory=cfg.buildSessionFactory();
		
		Session session=factory.openSession();
		
		User user=(User)session.get(User.class,Integer.valueOf(200));
		
		System.out.println(user);
		
		ByteArrayInputStream bis=new ByteArrayInputStream(user.getMyobj());
		ObjectInputStream ois=new ObjectInputStream(bis);
		
		Laddu laddu=(Laddu)ois.readObject();
		
		System.out.println(laddu);
	}
}
public class ReadClient1 {
 */

}
}
