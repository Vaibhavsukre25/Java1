package runall;

import java.io.FileInputStream;
import java.io.FileOutputStream;
import java.io.ObjectInputStream;
import java.io.ObjectOutputStream;
import java.io.Serializable;

public class IO7{
	public static void main(String[] args) throws Exception{
		/*
		 * Laddu laddu=new Laddu();
		
		System.out.println(laddu.size);
		laddu.size=laddu.size+4;
		
		FileOutputStream fos=new FileOutputStream("lad.dat");
		ObjectOutputStream oos=new ObjectOutputStream(fos);
		oos.writeObject(laddu);
		
		laddu.size=laddu.size-7;
		laddu.size=laddu.size+1;
		
		System.out.println(laddu.size);
		
		FileInputStream fis=new FileInputStream("lad.dat");
		ObjectInputStream ois=new ObjectInputStream(fis);
		
		Laddu laddu2=(Laddu)ois.readObject();
		System.out.println(laddu2.size);
	}
}
class Laddu implements Serializable{
	
	//transient int size=10;//remember transient variables are not serialized...
	int size=10;
}
		 */
	}
}